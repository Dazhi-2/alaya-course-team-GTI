# 迭代4 小组复盘汇总

**小组**：GTI全球特别行动组（模拟）  
**日期**：2026-06-05

## 综合总结
迭代4完成了成绩管理功能（录入、修改、统计、个人查看），采用简化版设计（Enrollment 追加字段）。所有核心业务流程通过手动验证，测试通过率 98%（2个非关键断言失败已记录）。

## 主要亮点
- 简化版设计降低了复杂度，符合业务直觉。
- 权限校验完善（课程归属 + active状态）。
- 数据库迁移策略：开发用 `ddl-auto: update`，测试用内存 H2。

## 主要困难与解决
| 困难 | 解决方法 |
|------|----------|
| 数据库文件损坏导致测试启动失败 | 配置测试专用内存数据库 |
| 小数成绩导致截断 | 增加 `Integer.parseInt()` 校验 |
| 成绩统计空列表仍显示0分 | 条件添加统计属性 |

## 下次改进
- 迭代5可考虑独立 Grade 实体（如需成绩历史追踪）。
- 生产环境务必使用 Flyway 管理数据库变更。