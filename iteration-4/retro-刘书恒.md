# 迭代4 个人复盘

**姓名**：刘书恒  
**日期**：2026-06-05

## 做得好的
1. 严格执行简化版设计（成绩字段直接放在 Enrollment 上），避免过度设计。
2. 权限校验三层：URL角色 + 课程归属 + active状态，确保安全。
3. 成绩统计空安全处理（无成绩时不显示统计区域）。
4. 测试自包含，使用内存数据库避免环境依赖。

## 遇到的困难
1. 测试开始时数据库文件损坏，改用 `application-test.yml` + 内存数据库解决。
2. 跨模型复审发现小数成绩截断问题，增加整数校验。
3. 成绩统计断言失败，调整测试数据与断言值一致。

## 经验与改进
- 开发环境与测试环境数据库分离：生产用 Flyway，测试用内存 H2。
- 跨模型复审对边界条件（小数、空列表）非常敏感，值得坚持。
- 成绩管理业务简单，简化版设计足够，无需独立 Grade 实体。

## 对跨模型 AI 协作的认知
Kimi 在复审时发现了“全部未录入”时统计区域仍显示0分的问题，这属于 UI 层面的边界条件，人工容易忽略。跨模型复审的价值在于用不同模型的“视角差异”发现盲区。